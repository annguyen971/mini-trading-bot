[project]
name = "core-lib"
version = "0.1.0"
dependencies = [
    "psycopg[binary]",
    "pydantic",
    "pandas",
    "numpy",
]

[project.optional-dependencies]
test = [
    "pytest",
    "psycopg2-binary",
]
